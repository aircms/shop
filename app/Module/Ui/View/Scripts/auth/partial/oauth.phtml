<?php

use App\Helper\Factory;

$googleAuthUrl = Factory::googleOAuth()?->authUrl();

if (!$googleAuthUrl) {
  return;
}

/** @var string $title */
$title = $this->title;

echo a(
  href: $googleAuthUrl,
  data: ['force' => true],
  class: 'p-20 d-b br-5 sc-level-1 fw-6 d-f jc-c gp-5',
  content: [
    img('images/google.svg'),
    $title
  ]
);


